
class CircULAR_QUEUE: 
    def __init__(self):
        self.stack1 = []
        # self stack2 - used to store items when stack1 is full
   
     
 	def push_front(self, item): 
      if len( self . stack 1 ) != 0: 
          self  . push( item ) 
  
		      	else:   self.push_back(item)                                                                                                                                                                                                                                                                                                                                                                                                                            if self   . is_full():　print( "Queue is Full")　　else :________________________________________________________print("Queue has space") 　　　　　
def isEmpty(circular_queue):return len (circularly_queued) ＝＝0__class Main:   　def main(CircularQueue):    while True:     print( “Enter”， “push” or “pop” ，”or” “exit” )    if input（）＝=“push”:　item 　= int（input（“Enter item to push：“））    queue.Push_Front(int(input(“enter item number to be pushed：”)))    elif input(）==”pop“:　queue_item_to_pop ＆＝int（输入（”enter the item you want to pop：“”）)     queue_push(queue, queue item_t_o_p)     elif 输入( )＝ 等于“退出”:  break    else：  print（ “Invalid input”）
if __name__ ＞＝＞＞：
main()
______________________
________
_______
______<>
__
_
如果你想要一个双端队列，你可以稍微修改一下代码：